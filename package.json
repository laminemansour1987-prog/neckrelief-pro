{
  "name": "plomberie-ia-agent",
  "version": "1.0.0",
  "description": "Agent IA qui gere les demandes de plomberie : diagnostic, devis et prise de rendez-vous.",
  "private": true,
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --watch --env-file-if-exists=.env server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "nodemailer": "^6.9.14"
  },
  "engines": {
    "node": ">=18"
  }
}
