# Port du serveur
PORT=3000

# Jeton simple pour proteger le tableau de bord admin (routes /api/appointments, /api/events, /api/stats)
ADMIN_TOKEN=changeme

# --- IA conversationnelle (optionnel) ---
# Sans cle, l'agent fonctionne quand meme avec des reponses pre-redigees de qualite.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929

# --- Calendrier / creneaux automatiques ---
# Determine les creneaux proposes par l'agent et evite tout double-booking.
WORK_START_HOUR=8
WORK_END_HOUR=18
SLOT_HOURS=2
MAX_CONCURRENT_JOBS=1
URGENT_BUFFER_MINUTES=45
NORMAL_BUFFER_HOURS=2

# --- Notification email automatique au plombier (optionnel) ---
# Renseignez ces 4 valeurs pour recevoir un email a chaque nouvelle demande.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
ADMIN_EMAIL=

# --- Confirmation SMS automatique au client (optionnel, via Twilio) ---
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
